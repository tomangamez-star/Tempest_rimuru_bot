{
  "name": "rimuru-casino",
  "version": "1.0.3",
  "description": "Rimuru Tempest — Telegram casino gambling bot (virtual coins only, no real money). Slots, dice, coinflip, mines, blackjack, roulette, higher/lower, lottery, robbery, heists & more.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node -r ./src/persistence-hardening.js --max-old-space-size=512 src/index.js",
    "dev": "node -r ./src/persistence-hardening.js --max-old-space-size=512 src/index.js",
    "test": "node tests/smoke.js && node tests/dashboard-test.js && node tests/backup-regression-test.js && node tests/attack-test.js && node tests/waifu-test.js && node tests/hunt-test.js",
    "test:pg": "node tests/hydrate-pg-test.js && node tests/pg-persist-test.js && node tests/boot-cycle-test.js && node tests/regression-lock-race-test.js && node tests/bandwidth-test.js",
    "migrate": "node scripts/migrate-sqlite-to-pg.js",
    "migrate:dry": "node scripts/migrate-sqlite-to-pg.js --dry-run",
    "check": "node --check src/index.js && for f in src/*.js src/dashboard/*.js src/games/*.js src/crimes/*.js tests/*.js scripts/*.js; do node --check \"$f\" || exit 1; done && echo 'ALL SYNTAX OK'"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": ["telegram", "bot", "casino", "gambling", "rimuru", "slime", "fun", "virtual-coins"],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "groq-sdk": "^0.9.0",
    "node-telegram-bot-api": "^0.66.0",
    "pg": "^8.23.0",
    "socket.io": "^4.8.3"
  }
}
